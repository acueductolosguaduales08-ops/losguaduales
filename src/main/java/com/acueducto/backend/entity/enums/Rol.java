package com.acueducto.backend.entity.enums;

/** Roles internos del sistema. El "Usuario Publico" no requiere autenticacion, por lo que no se modela como rol. */
public enum Rol {
    ASOCIADO,
    TESORERO,
    ADMINISTRADOR
}
